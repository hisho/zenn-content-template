{
  "name": "zenn-content",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "postinstall": "npx zenn init",
    "start": "run-p preview server",
    "preview": "npx zenn preview --port 65535",
    "server": "browser-sync start --config bs-config.js",
    "article": "npx zenn new:article",
    "book": "npx zenn new:book"
  },
  "dependencies": {
    "zenn-cli": "^0.1.32"
  },
  "devDependencies": {
    "asciify": "^1.3.5",
    "browser-sync": "^2.26.12",
    "npm-run-all": "^4.1.5"
  }
}
